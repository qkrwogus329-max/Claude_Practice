# CLAUDE.md

이 파일은 이 저장소에서 작업하는 Claude Code(claude.ai/code)에게 제공하는 가이드다.

## 프로젝트 현황

이 저장소는 아직 커밋된 코드가 없는 초기 상태다. 빌드 시스템, 테스트 스위트, 코드 구조가
갖춰지면 이 파일에 빌드/린트/테스트 명령과 상위 아키텍처 설명을 추가한다.

## 교육용 자료 안내 (중요)

`WP_Templates/`에는 SW 품질교육용 산출물 템플릿이 들어있으며 저작권은 Synetics에 있다
(`WP_Templates/Engineering/README.md`). `WP_Templates/PRC-TPL-001_표준 산출물 양식
등록부.xlsx`는 이 저장소가 교육용 가상 프로젝트(VJ-ECL-2026)이며, PC/SIL·Web 증거만
다루고 **HIL, 실차, 시스템 시험, 공식 A-SPICE 심사, ISO 26262 준수, 인증 또는 ASIL
달성을 주장하지 않는다**고 명시한다. 이 저장소에서 만드는 모든 산출물은 표준의 구조와
원칙을 학습·적용하는 목적이며, 실제 인증/공식 준수를 주장하지 않는다. 템플릿 자료를
저장소 밖으로 배포·게시하지 않는다.

## 소프트웨어 개발 정보
이 소프트웨어의 개발은 다음의 항목을 이용하여 개발한다.
 - Python 3.14 를 이용해서 개발한다.
 - 코드 테스트는 Unittest를 활용한다.
 - 순환 복잡도, 함수 라인 수 등의 측정 지표는 오픈소스 도구를 이용한다.

## 프로젝트 개발 정책

### 개발 생명 주기

- 이 프로젝트는 **반드시** 분석, 설계, 구현, 테스트의 순서로 개발을 진행한다.
- 각 단계가 완료되었을 때, 지정된 템플릿을 이용한 산출물이 생성되어야 한다. 산출물
  템플릿은 `WP_Templates/Engineering/`(공정별 하위 폴더)에 있으며, 템플릿 ID와 적용
  산출물 매핑은 `WP_Templates/PRC-TPL-001_표준 산출물 양식 등록부.xlsx`를 따른다.

### 분석 지침
 - 요구사항 분석 단계 수행은 requirements-analyst 가 담당한다.

### 아키텍처 설계 지침
 - 아키텍처 설계 단계 수행은 architecture-designer 가 담당한다

### 구현 지침
 - 구현 단계 수행은 coding 서브에이전트가 담당한다.
 - TDD 방식으로 진행하고, TDD 스킬을 사용해야 한다.
 - 다음의 품질 지표를 **반드시** 준수해야 한다.
    - 함수 라인수는 순수 코드라인 기준으로 50라인 이하여야 함.
    - 함수 순환 복잡도는 10 이하여야 한다.
    - 중복 코드는 7라인까지만 허용한다.
    - 주석은 Doxygen 방식으로 작성하고, LOC의 20% 이상의 비율로 작성해야 한다.
 - 함수명, 변수명은 3글자 이상으로 사용하고, 카멜 표기법을 활용한다.


### 요구사항 작성 정책

- 모든 요구사항은 **ISO 26262**와 **A-SPICE** 표준을 준수해야 한다.
- **기능 요구사항**은 UML/SysML 다이어그램으로 표현 가능해야 한다 (예: Use Case Diagram,
  Sequence Diagram, State Machine Diagram, SysML Requirement Diagram 등).
- **비기능 요구사항**은 ISO 25010의 품질 특성(기능 적합성, 성능 효율성, 호환성, 사용성,
  신뢰성, 보안성, 유지보수성, 이식성)에 따라 분류하여 작성하고, 각 요구사항마다 **실행
  가능한(실제로 측정·시험 가능한) 검증 방안**을 함께 제시한다. 실행 불가능하거나 막연한
  검증 방안(예: "사용자가 만족해야 한다")은 제시하지 않는다.
- 모든 요구사항은 사람이 이해하는 데 문제가 없도록 **명확하게** 작성한다 — 모호한 표현,
  이중 부정, 정의되지 않은 용어를 피한다.
- 모든 요구사항은 이후 산출물(설계, 구현, 테스트 등)과 **양방향 추적**이 가능해야 한다.
  - **추적 방안**: 모든 요구사항에 고유 ID(`REQ-<영역>-<일련번호>`, 예: `REQ-SYS-001`,
    `REQ-SWE-001`)를 부여한다. 하위 산출물(설계 요소, 코드, 테스트 케이스)은 자신이
    만족/검증하는 요구사항 ID를 명시적으로 참조한다. 요구사항 ↔ 설계 ↔ 구현 ↔ 테스트 간
    매핑은 `WP_Templates/Engineering/Traceability/TPL-TRC-001_양방향 요구사항 추적
    매트릭스 템플릿.xlsx`(실제 산출물: `ENG-TRC-001`)로 유지하며, 요구사항이나 하위
    산출물이 변경될 때마다 매트릭스를 함께 갱신한다.
  - 새 요구사항이나 산출물을 작성할 때는 이 추적 방안(ID 부여, 상호 참조, 매트릭스 갱신)을
    실제로 적용한다.
- 모든 요구사항은 서로 모순되지 않도록 **일관성**을 확보한다 — 동일 대상에 대해 서로 다른
  값/조건을 요구하지 않는지 교차 검토한다.

### 아키텍처 설계 정책

- **A-SPICE**와 **ISO 26262 Part 6**의 아키텍처 설계 원칙을 준수한다.
- 모든 컴포넌트는 **반드시** 높은 응집력과 낮은 결합도를 유지한다.
- 변경에 유연하게 대응할 수 있도록 설계한다(가변점을 인터페이스/플러그인 구조로 분리).
- **SOLID 원칙**(SRP/OCP/LSP/ISP/DIP)을 준수한다.
- 컴포넌트의 인터페이스를 **반드시** 정의하고, 컴포넌트 간 통신은 **오직 인터페이스를
  통해서만** 이루어지도록 한다.
- 컴포넌트의 통합 순서를 정의한다(의존성 그래프 기반, 스텁/드라이버·회귀 범위 포함).
- 아키텍처를 설계할 때는 적절한 아키텍처 구조(후보)를 먼저 제안하고, 사용자가 선택하게
  한다 — 임의로 하나를 확정하지 않는다.
- 산출물은 사용자가 제시한 아키텍처 설계서 양식(`WP_Templates/Engineering/
  SoftwareArchitecturalDesign/`의 템플릿)에 따라 작성한다.